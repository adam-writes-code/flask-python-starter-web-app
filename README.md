# flask-python-starter-web-app

My first web app built with Python and Flask while following the Coursera course **Create Your First Web App with Python and Flask**.

## What it does

This is a simple Flask app with two routes:

- `/`
- `/index`

Both display a basic "Hello World!" message.

## How to run

1. Clone the repository
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
